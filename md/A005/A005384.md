# Sophie Germain primes p: 2p\+1 is also prime\.
https://oeis.org/A005384
## JSON
```JSON
[2, 3, 5, 11, 23, 29, 41, 53, 83, 89, 113, 131, 173, 179, 191, 233, 239, 251, 281, 293, 359, 419, 431, 443, 491, 509, 593, 641, 653, 659, 683, 719, 743, 761, 809, 911, 953, 1013, 1019, 1031, 1049, 1103, 1223, 1229, 1289, 1409, 1439, 1451, 1481, 1499, 1511, 1559]
```
## Maple
```Maple
A:={}: for n from 1 to 246 do if isprime(2*ithprime(n)+1) then A:=A union {ithprime(n)} fi od: A:=A; # _Emeric Deutsch_, Dec 09 2004
```
## Mathematica
```Mathematica
Select[Prime[Range[1000]],PrimeQ[2#+1]&]
```
```Mathematica
lst = {}; Do[If[PrimeQ[n + 1] && PrimeOmega[n] == 2, AppendTo[lst, n/2]], {n, 2, 10^4}]; lst (* _Hilko Koning_, Aug 17 2021 *)
```
## Magma
```Magma
[ p: p in PrimesUpTo(1560) | IsPrime(2*p+1) ]; // _Klaus Brockhaus_, Jan 01 2009
```
## PARI
```PARI
select(p->isprime(2*p+1), primes(1000)) \\ In old PARI versions <= 2.4.2, use select(primes(1000), p->isprime(2*p+1)).
```
```PARI
forprime(n=2, 10^3, if(ispseudoprime(2*n+1), print1(n, ", "))) \\ _Felix Fröhlich_, Jun 15 2014
```
```PARI
is_A005384=(p->isprime(2*p+1)&&isprime(p));
  {A005384_vec(N=100,p=1)=vector(N,i,until(isprime(2*p+1),p=nextprime(p+1));p)} \\ _M. F. Hasler_, Mar 03 2020
```
## GAP
```GAP
Filtered([1..1600],p->IsPrime(p) and IsPrime(2*p+1)); # _Muniru A Asiru_, Mar 06 2019
```
## Python
```Python
from sympy import isprime, nextprime
def ok(p): return isprime(2*p+1)
def aupto(limit): # only test primes
  alst, p = [], 2
  while p <= limit:
    if ok(p): alst.append(p)
    p = nextprime(p)
  return alst
print(aupto(1559)) # _Michael S. Branicky_, Feb 03 2021
```

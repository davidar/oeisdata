# Summarize the previous term \(digits in increasing order\), starting with a\(1\) \= 1\.
https://oeis.org/A005151
## JSON
```JSON
[1, 11, 21, 1112, 3112, 211213, 312213, 212223, 114213, 31121314, 41122314, 31221324, 21322314, 21322314, 21322314, 21322314, 21322314, 21322314, 21322314, 21322314, 21322314, 21322314, 21322314, 21322314, 21322314, 21322314, 21322314, 21322314, 21322314]
```
## Mathematica
```Mathematica
RunLengthEncode[x_List] := (Through[{Length, First}[ #1]] &) /@ Split[ Sort[x]]; LookAndSay[n_, d_:1] := NestList[ Flatten[ RunLengthEncode[ # ]] &, {d}, n - 1]; F[n_] := LookAndSay[n, 1][[n]]; Table[ FromDigits[ F[n]], {n, 25}] (* _Robert G. Wilson v_, Jan 22 2004 *)
```
```Mathematica
a[1] = 1; a[n_] := a[n] = FromDigits[Reverse /@ Sort[Tally[a[n-1] // IntegerDigits], #1[[1]] < #2[[1]]&] // Flatten]; Array[a, 26] (* _Jean-François Alcover_, Jan 25 2016 *)
```
## Haskell
```Haskell
import Data.List (group, sort, transpose)
a005151 n = a005151_list !! (n-1)
a005151_list = 1 : f [1] :: [Integer] where
   f xs = (read $ concatMap show ys) : f ys where
          ys = concat $ transpose [map length zss, map head zss]
          zss = group $ sort xs
-- _Reinhard Zumkeller_, Jan 25 2014
```
## PARI
```PARI
say(n) = {digs = digits(n); d = vecsort(digs,,8); s = ""; for (k=1, #d, nbk = #select(x->x==d[k], digs); s = concat(s, Str(nbk)); s = concat(s, d[k]);); eval(s);}
lista(nn) = {print1(n = 1, ", "); for (k=1, nn, m = say(n); print1(m, ", "); n = m;);} \\ _Michel Marcus_, Feb 12 2016
```
```PARI
a(n,show_all=1,a=1)={for(i=2,n,show_all&&print1(a",");a=A047842(a));a} \\ _M. F. Hasler_, Feb 25 2018
```
```PARI
Vec(x*(1 + 10*x + 10*x^2 + 1091*x^3 + 2000*x^4 + 208101*x^5 + 101000*x^6 - 99990*x^7 - 98010*x^8 + 31007101*x^9 + 10001000*x^10 - 9900990*x^11 - 9899010*x^12) / (1 - x) + O(x^40)) \\ _Colin Barker_, Aug 23 2018
```
## Python
```Python
from itertools import accumulate, groupby, repeat
def summarize(n, _):
  return int("".join(str(len(list(g)))+k for k, g in groupby(sorted(str(n)))))
def aupton(nn): return list(accumulate(repeat(1, nn+1), summarize))
print(aupton(25)) # _Michael S. Branicky_, Jan 11 2021
```

# Expansion of Product\_\{m \>\= 1\} \(1 \+ x^m\); number of partitions of n into distinct parts; number of partitions of n into odd parts\.
https://oeis.org/A000009
## JSON
```JSON
[1, 1, 1, 2, 2, 3, 4, 5, 6, 8, 10, 12, 15, 18, 22, 27, 32, 38, 46, 54, 64, 76, 89, 104, 122, 142, 165, 192, 222, 256, 296, 340, 390, 448, 512, 585, 668, 760, 864, 982, 1113, 1260, 1426, 1610, 1816, 2048, 2304, 2590, 2910, 3264, 3658, 4097, 4582, 5120, 5718, 6378]
```
## Maple
```Maple
N := 100; t1 := series(mul(1+x^k,k=1..N),x,N); A000009 := proc(n) coeff(t1,x,n); end;
spec := [ P, {P=PowerSet(N), N=Sequence(Z,card>=1)} ]: [ seq(combstruct[count](spec, size=n), n=0..58) ];
spec := [ P, {P=PowerSet(N), N=Sequence(Z,card>=1)} ]: combstruct[allstructs](spec, size=10); # to get the actual partitions for n=10
A000009 := proc(n)
    local x,m;
    product(1+x^m,m=1..n+1) ;
    expand(%) ;
    coeff(%,x,n) ;
end proc: # _R. J. Mathar_, Jun 18 2016
# Alternatively:
simplify(expand(QDifferenceEquations:-QPochhammer(-1,x,99)/2,x)):
seq(coeff(%,x,n), n=0..55); # _Peter Luschny_, Nov 17 2016
```
## Mathematica
```Mathematica
PartitionsQ[Range[0, 60]] (* _Harvey Dale_, Jul 27 2009 *)
```
```Mathematica
a[ n_] := SeriesCoefficient[ Product[ 1 + x^k, {k, n}], {x, 0, n}]; (* _Michael Somos_, Jul 06 2011 *)
```
```Mathematica
a[ n_] := SeriesCoefficient[ 1 / Product[ 1 - x^k, {k, 1, n, 2}], {x, 0, n}]; (* _Michael Somos_, Jul 06 2011 *)
```
```Mathematica
a[ n_] := With[ {t = Log[q] / (2 Pi I)}, SeriesCoefficient[ q^(-1/24) DedekindEta[2 t] / DedekindEta[ t], {q, 0, n}]]; (* _Michael Somos_, Jul 06 2011 *)
```
```Mathematica
a[ n_] := SeriesCoefficient[ 1 / QPochhammer[ x, x^2], {x, 0, n}]; (* _Michael Somos_, May 24 2013 *)
```
```Mathematica
a[ n_] := SeriesCoefficient[ Series[ QHypergeometricPFQ[ {q}, {q x}, q, - q x], {q, 0, n}] /. x -> 1, {q, 0, n}]; (* _Michael Somos_, Mar 04 2014 *)
```
```Mathematica
a[ n_] := SeriesCoefficient[ QHypergeometricPFQ[{}, {}, q, -1] / 2, {q, 0, n}]; (* _Michael Somos_, Mar 04 2014 *)
```
```Mathematica
nmax = 60; CoefficientList[Series[Exp[Sum[(-1)^(k+1)/k*x^k/(1-x^k), {k, 1, nmax}]], {x, 0, nmax}], x] (* _Vaclav Kotesovec_, Aug 25 2015 *)
```
```Mathematica
nmax = 100; poly = ConstantArray[0, nmax + 1]; poly[[1]] = 1; poly[[2]] = 1; Do[Do[poly[[j + 1]] += poly[[j - k + 1]], {j, nmax, k, -1}];, {k, 2, nmax}]; poly (* _Vaclav Kotesovec_, Jan 14 2017 *)
```
## PARI
```PARI
{a(n) = if( n<0, 0, polcoeff( prod( k=1, n, 1 + x^k, 1 + x * O(x^n)), n))}; /* _Michael Somos_, Nov 17 1999 */
```
```PARI
{a(n) = my(A); if( n<0, 0, A = x * O(x^n); polcoeff( eta(x^2 + A) / eta(x + A), n))};
```
```PARI
{a(n) = my(c); forpart(p=n, if( n<1 || p[1]<2, c++; for(i=1, #p-1, if( p[i+1] > p[i]+1, c--; break)))); c}; /* _Michael Somos_, Aug 13 2017 */
```
```PARI
lista(nn) = {q='q+O('q^nn); Vec(eta(q^2)/eta(q))} \\ _Altug Alkan_, Mar 20 2018
```
## Magma
```Magma
Coefficients(&*[1+x^m:m in [1..100]])[1..100] where x is PolynomialRing(Integers()).1; // Sergei Haller (sergei(AT)sergei-haller.de), Dec 21 2006
```
## Haskell
```Haskell
import Data.MemoCombinators (memo2, integral)
a000009 n = a000009_list !! n
a000009_list = map (pM 1) [0..] where
   pM = memo2 integral integral p
   p _ 0 = 1
   p k m | m < k     = 0
         | otherwise = pM (k + 1) (m - k) + pM (k + 1) m
-- _Reinhard Zumkeller_, Sep 09 2015, Nov 05 2013
```
## Maxima
```Maxima
num_distinct_partitions(60,list); /* _Emanuele Munarini_, Feb 24 2014 */
```
```Maxima
h(n):=if oddp(n)=true then 1 else 0;
S(n,m):=if n=0 then 1 else if n<m then 0 else if n=m then h(n) else sum(h(k)*S(n-k,k),k,m,n/2)+h(n);
makelist(S(n,1),n,0,27); /* _Vladimir Kruchinin_, Sep 07 2014 */
```
## Sage
```Sage
# uses[EulerTransform from A166861]
a = BinaryRecurrenceSequence(0, 1)
b = EulerTransform(a)
print([b(n) for n in range(56)]) # _Peter Luschny_, Nov 11 2020
```
## Python
```Python
# uses A010815
from functools import lru_cache
from math import isqrt
@lru_cache(maxsize=None)
def A000009(n): return 1 if n == 0 else A010815(n)+2*sum((-1)**(k+1)*A000009(n-k**2) for k in range(1,isqrt(n)+1)) # _Chai Wah Wu_, Sep 08 2021
```
## Julia
```Julia
# uses A010815
using Memoize
@memoize function A000009(n)
    n == 0 && return 1
    s = sum((-1)^k*A000009(n - k^2) for k in 1:isqrt(n))
    A010815(n) - 2*s
end # _Peter Luschny_, Sep 09 2021
```

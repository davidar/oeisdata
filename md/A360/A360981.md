# a\(n\) is the least positive multiple of n that is an evil number \(A001969\)\.
https://oeis.org/A360981
## JSON
```JSON
[3, 6, 3, 12, 5, 6, 63, 24, 9, 10, 33, 12, 39, 126, 15, 48, 17, 18, 57, 20, 63, 66, 23, 24, 75, 78, 27, 252, 29, 30, 1023, 96, 33, 34, 105, 36, 111, 114, 39, 40, 123, 126, 43, 132, 45, 46, 141, 48, 147, 150, 51, 156, 53, 54, 165, 504, 57, 58, 177, 60, 183, 2046]
```
## Maple
```Maple
f:= proc(n) local k;
  for k from n by n do
    if convert(convert(k,base,2),`+`)::even then return k fi
  od
end proc:
map(f, [$1..100]); # _Robert Israel_, Mar 29 2023
```
## Mathematica
```Mathematica
a[n_] := Module[{k = n}, While[OddQ[DigitCount[k, 2, 1]], k +=n]; k]; Array[a, 100] (* _Amiram Eldar_, Aug 07 2023 *)
```
## PARI
```PARI
a(n) = { forstep (m=n, oo, n, if (hammingweight(m)%2==0, return (m))) }
```
## Python
```Python
def A360981(n):
    k = n
    while k.bit_count()&1:
        k += n
    return k # _Chai Wah Wu_, Feb 28 2023
```

# Quarter\-squares: a\(n\) \= floor\(n/2\)\*ceiling\(n/2\)\. Equivalently, a\(n\) \= floor\(n^2/4\)\.
https://oeis.org/A002620
## JSON
```JSON
[0, 0, 1, 2, 4, 6, 9, 12, 16, 20, 25, 30, 36, 42, 49, 56, 64, 72, 81, 90, 100, 110, 121, 132, 144, 156, 169, 182, 196, 210, 225, 240, 256, 272, 289, 306, 324, 342, 361, 380, 400, 420, 441, 462, 484, 506, 529, 552, 576, 600, 625, 650, 676, 702, 729, 756, 784, 812]
```
## Maple
```Maple
A002620 := n->floor(n^2/4); G002620 := series(x^2/((1-x)^2*(1-x^2)),x,60);
with(combstruct):ZL:=[st,{st=Prod(left,right),left=Set(U,card=r),right=Set(U,card<r),U=Sequence(Z,card>=1)}, unlabeled]: subs(r=1,stack): seq(count(subs(r=2,ZL),size=m),m=0..57) ; # _Zerinvary Lajos_, Mar 09 2007
```
## Mathematica
```Mathematica
Table[Ceiling[n/2] Floor[n/2], {n, 0, 56}] (* _Robert G. Wilson v_, Jun 18 2005 *)
```
```Mathematica
LinearRecurrence[{2, 0, -2, 1}, {0, 0, 1, 2}, 60] (* _Harvey P. Dale_, Oct 05 2012 *)
```
```Mathematica
Table[Floor[n^2/4], {n, 0, 20}] (* _Eric W. Weisstein_, Sep 11 2018 *)
```
```Mathematica
Floor[Range[0, 20]^2/4] (* _Eric W. Weisstein_, Sep 11 2018 *)
```
```Mathematica
CoefficientList[Series[-(x^2/((-1 + x)^3 (1 + x))), {x, 0, 20}], x] (* _Eric W. Weisstein_, Sep 11 2018 *)
```
```Mathematica
Table[Floor[n^2/2]/2, {n, 0, 56}] (* _Clark Kimberling_, Dec 05 2021 *)
```
## Magma
```Magma
[ Floor(n/2)*Ceiling(n/2) : n in [0..40]];
```
## PARI
```PARI
a(n)=n^2\4
```
```PARI
(t(n)=n*(n+1)/2);for(i=1,50,print1(",",(-1)^i*sum(k=1,i,(-1)^k*t(k))))
```
```PARI
a(n)=n^2>>2 \\ _Charles R Greathouse IV_, Nov 11 2009
```
```PARI
x='x+O('x^100); concat([0, 0], Vec(x^2/((1-x)^2*(1-x^2)))) \\ _Altug Alkan_, Oct 15 2015
```
## Haskell
```Haskell
a002620 = (`div` 4) . (^ 2) -- _Reinhard Zumkeller_, Feb 24 2012
```
## Maxima
```Maxima
makelist(floor(n^2/4),n,0,50); /* _Martin Ettl_, Oct 17 2012 */
```
## Sage
```Sage
def A002620():
     x, y = 0, 1
     yield x
     while true:
         yield x
         x, y = x + y, x//y + 1
a = A002620(); print([next(a) for i in range(58)]) # _Peter Luschny_, Dec 17 2015
```
## GAP
```GAP
# using the formula by Paul Barry
A002620 := List([1..10^4], n-> (2*n^2 - 1 + (-1)^n)/8); # _Muniru A Asiru_, Feb 01 2018
```
## Python
```Python
def A002620(n): return (n**2)>>2 # _Chai Wah Wu_, Jul 07 2022
```

# a\(n\) is the number of squares k^2 with n^2 <\= k^2 <\= n^3\.
https://oeis.org/A349993
## JSON
```JSON
[1, 1, 1, 3, 5, 7, 9, 12, 15, 19, 22, 26, 30, 34, 39, 44, 49, 54, 59, 64, 70, 76, 82, 88, 94, 101, 107, 114, 121, 128, 135, 142, 150, 157, 165, 173, 181, 189, 197, 205, 213, 222, 231, 239, 248, 257, 266, 276, 285, 295, 304, 314, 323, 333, 343, 353, 364, 374, 384, 395, 405]
```
## Mathematica
```Mathematica
Table[Floor[Sqrt[n^3]]-n+1,{n,0,100}] (* _Giorgos Kalogeropoulos_, Dec 08 2021 *)
```
## PARI
```PARI
a(n) = sqrtint(n^3) - n + 1
```
```PARI
a(n)=sum(k=n^2,n^3,issquare(k))
```
```PARI
for(n=0,60,my(n2=n^2,n3=n^3);print1(sum(k=n2,n3,issquare(k)),", "))
```
## Python
```Python
def a(n):
    counter = 0
    while (n+counter)**2 <= n**3: counter += 1
    return (counter)
print([a(n) for n in range(0,60)])
```
```Python
from math import isqrt
def A349993(n): return isqrt(n**3) - n + 1 # _Chai Wah Wu_, Dec 08 2021
```

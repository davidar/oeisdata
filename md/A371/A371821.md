# Composite numbers which divide the concatenation of their ascending ordered prime factors, with repetition, when written in binary\.
https://oeis.org/A371821
## JSON
```JSON
[85329, 177904587, 333577497]
```
## Python
```Python
from itertools import count, islice
from sympy import factorint
def A371821_gen(startvalue=1): # generator of terms >= startvalue
    for n in count(max(startvalue,1)):
        f = sorted(factorint(n,multiple=True))
        if len(f) > 1:
            c = 0
            for p in f:
                c = ((c<<p.bit_length())+p)%n
            if not c:
                yield n
A371821_list = list(islice(A371821_gen(),3)) # _Chai Wah Wu_, Apr 11 2024
```
```Python
from sympy import factorint, isprime
def ok(n): return not isprime(n) and int("".join(bin(p)[2:]*e for p, e in factorint(n).items()), 2)%n == 0 # _Michael S. Branicky_, Apr 12 2024
```

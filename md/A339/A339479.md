# Number of partitions consisting of n parts, each of which is a power of 2, where one part is 1 and no part is more than twice the sum of all smaller parts\.
https://oeis.org/A339479
## JSON
```JSON
[1, 2, 5, 13, 35, 95, 259, 708, 1938, 5308, 14543, 39852, 109216, 299326, 820378, 2248484, 6162671, 16890790, 46294769, 126886206, 347774063, 953191416, 2612541157, 7160547089, 19625887013, 53791344195, 147433273080, 404090482159, 1107545909953, 3035602173663]
```
## Maple
```Maple
b:= proc(n, t) option remember; `if`(n=0, 1,
     `if`(t=0, 0, b(n, iquo(t, 2))+b(n-1, t+2)))
    end:
a:= n-> b(n, 1):
seq(a(n), n=1..30);  # _Alois P. Heinz_, Apr 27 2021
```
## Mathematica
```Mathematica
b[n_, t_] := b[n, t] = If[n == 0, 1, If[t == 0, 0, b[n, Quotient[t, 2]] + b[n - 1, t + 2]]];
```
```Mathematica
a[n_] := b[n, 1];
```
```Mathematica
Table[a[n], {n, 1, 30}] (* _Jean-François Alcover_, Jul 07 2021, after _Alois P. Heinz_ *)
```
## PARI
```PARI
seq(n)={my(v=vector(n), a=vector(n)); a[1]=v[1]=1; for(n=2, n, for(j=1, n-1, v[n-(n-j)\2] += v[j]); a[n]=vecsum(v)); a} \\ _Andrew Howroyd_, Apr 25 2021
```
## Python
```Python
from functools import cache
@cache
def r339479(n, k):
    if n == 0:
        return 1
    elif k == 0:
        return r339479(n - 1, 1)
    else:
        return r339479(n - 1, k + 1) + r339479(n, k // 2)
def a339479(n): return r339479(n,0)
print([a339479(n) for n in range(1, 100)])
```

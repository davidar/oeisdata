# Primes p such that A007088\(p\) \=\= 1 \(mod p\)\.
https://oeis.org/A339566
## JSON
```JSON
[5, 137, 3967, 25087, 242899421]
```
## Maple
```Maple
p:= 1: R:= NULL:
while p < 3*10^8 do
p:= nextprime(p);
if convert(p,binary) mod p = 1 then R:= R, p fi
od:
R;
```
## Python
```Python
from sympy import nextprime
A339566_list, p = [], 2
while p < 10**10:
    if int(bin(p)[2:]) % p == 1:
        A339566_list.append(p)
    p = nextprime(p) # _Chai Wah Wu_, Dec 14 2020
```

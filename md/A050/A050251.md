# Number of palindromic primes less than 10^n\.
https://oeis.org/A050251
## JSON
```JSON
[0, 4, 5, 20, 20, 113, 113, 781, 781, 5953, 5953, 47995, 47995, 401696, 401696, 3438339, 3438339, 30483565, 30483565, 269577430, 269577430, 2427668363, 2427668363, 22170468927, 22170468927]
```
## Python
```Python
from __future__ import division
from sympy import isprime
def paloddgen(l,b=10): # generator of odd-length palindromes in base b of length <= 2*l
    if l > 0:
        yield 0
        for x in range(1,l+1):
            n = b**(x-1)
            n2 = n*b
            for y in range(n,n2):
                k, m = y//b, 0
                while k >= b:
                    k, r = divmod(k,b)
                    m = b*m + r
                yield y*n + b*m + k
def A050251(n):
    if n <= 1:
        return 4*n
    else:
        c = 1
        for i in paloddgen((n+1)//2):
            if isprime(i):
                c += 1
        return c # _Chai Wah Wu_, Jan 05 2015
```

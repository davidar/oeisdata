# Numbers that are palindromic in base 8\.
https://oeis.org/A029803
## JSON
```JSON
[0, 1, 2, 3, 4, 5, 6, 7, 9, 18, 27, 36, 45, 54, 63, 65, 73, 81, 89, 97, 105, 113, 121, 130, 138, 146, 154, 162, 170, 178, 186, 195, 203, 211, 219, 227, 235, 243, 251, 260, 268, 276, 284, 292, 300, 308, 316, 325, 333, 341, 349, 357, 365, 373, 381, 390, 398]
```
## Mathematica
```Mathematica
f[n_,b_] := Module[{i=IntegerDigits[n,b]}, i==Reverse[i]]; lst={}; Do[If[f[n,8], AppendTo[lst,n]], {n,1000}]; lst (* _Vladimir Joseph Stephan Orlovsky_, Jul 08 2009 *)
```
## PARI
```PARI
ispal(n,b=8)=my(d=digits(n,b)); d==Vecrev(d) \\ _Charles R Greathouse IV_, May 03 2020
```
## Python
```Python
from itertools import chain, count, islice
def A029803_gen(): # generator of terms
    return chain((0,),chain.from_iterable(chain((int((s:=oct(d)[2:])+s[-2::-1],8) for d in range(8**l,8**(l+1))), (int((s:=oct(d)[2:])+s[::-1],8) for d in range(8**l,8**(l+1)))) for l in count(0)))
A029803_list = list(islice(A029803_gen(),20)) # _Chai Wah Wu_, Jun 23 2022
```
```Python
def A029803(n):
    if n == 1: return 0
    y = (x:=1<<(m:=n.bit_length()-2)-m%3)<<3
    return (c:=n-x)*x+int(oct(c)[-2:1:-1]or'0',8) if n<x+y else (c:=n-y)*y+int(oct(c)[-1:1:-1]or'0',8) # _Chai Wah Wu_, Jun 13 2024
```

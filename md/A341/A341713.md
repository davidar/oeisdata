# Indices of Ennesrem primes: k such that A004094\(k\)\-1 is prime\.
https://oeis.org/A341713
## JSON
```JSON
[2, 3, 13, 21, 347, 1217, 1267, 16459, 100909, 342243]
```
## PARI
```PARI
for(n=1,10000,my(pe=fromdigits(Vecrev(digits(2^n)))-1);if(ispseudoprime(pe),print1(n,", "))) \\ _Hugo Pfoertner_, Feb 20 2021
```
## Python
```Python
from sympy import isprime
def ok(k): return isprime(int(str(2**k)[::-1]) - 1)
for k in range(1, 2*10**3):
  if ok(k): print(k, end=", ") # _Michael S. Branicky_, Feb 20 2021
```

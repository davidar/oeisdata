# a\(n\) \= smallest prime of the form n\|\|n\+1\|\|n\+2\|\|\.\.\.\|\|n\+k, where \|\| denotes decimal concatenation, or \-1 if no such prime exists\.
https://oeis.org/A341715
## JSON
```JSON
[2, 3, 4567, 5, 67, 7, 89]
```
## Mathematica
```Mathematica
Array[Block[{k = #, s = #}, While[! PrimeQ[s], k++; s = FromDigits[IntegerDigits[s]~Join~IntegerDigits[k]]]; s] &, 8, 2] (* _Michael De Vlieger_, Feb 22 2021 *)
```
## Python
```Python
from sympy import isprime
def A341715(n):
    m, k = n, n
    while not isprime(m):
        k += 1
        m = int(str(m)+str(k))
    return m # _Chai Wah Wu_, Feb 22 2021
```
## PARI
```PARI
A341715(n)=if(isprime(n),n,eval(concat([Str(k)|k<-[n..A084559(n)]]))) \\ _M. F. Hasler_, Feb 22 2021
```

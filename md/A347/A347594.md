# a\(0\) \= 1; for n\>0, a\(n\) is the smallest positive integer such that a\(n\-1\)^2 \+ n^2 \+ a\(n\) is a square\.
https://oeis.org/A347594
## JSON
```JSON
[1, 2, 1, 6, 12, 27, 19, 31, 64, 48, 96, 72, 1, 26, 28, 15, 3, 26, 24, 24, 48, 64, 44, 35, 48, 96, 108, 151, 131, 223, 447, 831, 639, 190, 380, 299, 507, 663, 1212, 904, 209, 7, 36, 104, 17, 87, 116, 211, 264, 264, 165, 103, 143, 151, 204, 303, 536, 1055, 860, 1688, 3156, 2592, 1341, 1399]
```
## Mathematica
```Mathematica
Nest[Append[#, Block[{k = 1, m = Last[#1]}, While[! IntegerQ@ Sqrt[#2^2 + m^2 + k], k++]; k]] & @@ {#, Length@ #} &, {1}, 63] (* _Michael De Vlieger_, Sep 08 2021 *)
```
## Python
```Python
from math import isqrt
A347594_list = [1]
for n in range(1,10**3):
    m = A347594_list[n-1]**2+n**2
    A347594_list.append((isqrt(m)+1)**2-m) # _Chai Wah Wu_, Sep 12 2021
```
## PARI
```PARI
lista(nn) = {my(prec = 1, list=List()); listput(list, prec); for (n=1, nn, my(k = 1); while (!issquare(prec^2+n^2+k), k++); listput(list, k); prec = k;); Vec(list);} \\ _Michel Marcus_, Sep 13 2021
```

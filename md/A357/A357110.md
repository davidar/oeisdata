# Numbers k such that 1 \+ k^2 \* 2^k \+ k^3 \* 3^k is prime\.
https://oeis.org/A357110
## JSON
```JSON
[2, 4, 6, 10, 12, 28, 30, 52, 60, 1170, 1292, 1882, 4760, 5160, 8388, 14652, 37700]
```
## Mathematica
```Mathematica
p = 31000
```
```Mathematica
ParallelTable[
```
```Mathematica
If[PrimeQ[1 + n^2*2^n + n^3*3^n], n, Nothing], {n, 0, p}]
```
## PARI
```PARI
is(n)=ispseudoprime(1 + n^2*2^n + n^3*3^n)
```
## Magma
```Magma
[k: k in [1..31000] | IsPrime(1 + k^2*2^k + k^3*3^k)];
```
## Python
```Python
from sympy import isprime
print([k for k in range(52000) if isprime(1 + (2**k)*(k**2) + (3**k)*(k**3))])
```

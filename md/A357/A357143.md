# a\(n\) is sum of the base\-5 digits of n each raised to the number of digits of n in base 5\.
https://oeis.org/A357143
## JSON
```JSON
[1, 2, 3, 4, 1, 2, 5, 10, 17, 4, 5, 8, 13, 20, 9, 10, 13, 18, 25, 16, 17, 20, 25, 32, 1, 2, 9, 28, 65, 2, 3, 10, 29, 66, 9, 10, 17, 36, 73, 28, 29, 36, 55, 92, 65, 66, 73, 92, 129, 8, 9, 16, 35, 72, 9, 10, 17, 36, 73, 16, 17, 24, 43, 80, 35, 36, 43, 62, 99, 72, 73, 80, 99, 136, 27]
```
## Maple
```Maple
f:= proc(n) local L,d,i;
  L:= convert(n,base,5);
  d:= nops(L);
  add(L[i]^d,i=1..d)
end proc:
map(f,[$1..100]); # _Robert Israel_, Oct 26 2023
```
## Mathematica
```Mathematica
a[n_] := Total[IntegerDigits[n, 5]^IntegerLength[n, 5]]; Array[a, 100] (* _Amiram Eldar_, Oct 30 2022 *)
```
## PARI
```PARI
a(n) = my(d=digits(n, 5)); sum(k=1, #d, d[k]^#d); \\ _Michel Marcus_, Oct 29 2022
```
## Python
```Python
from sympy.ntheory.factor_ import digits
def A357143(n):
    t = len(s:=digits(n,5)[1:])
    return sum(d**t for d in s) # _Chai Wah Wu_, Oct 31 2022
```

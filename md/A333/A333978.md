# Numbers of the form b\_1 \* b\_2 \* \.\.\. \* b\_t, where b\_1 \= 1 and b\_\(i \+ 1\) \- b\_i \= 0 or 1\.
https://oeis.org/A333978
## JSON
```JSON
[1, 2, 4, 6, 8, 12, 16, 18, 24, 32, 36, 48, 54, 64, 72, 96, 108, 120, 128, 144, 162, 192, 216, 240, 256, 288, 324, 360, 384, 432, 480, 486, 512, 576, 600, 648, 720, 768, 864, 960, 972, 1024, 1080, 1152, 1200, 1296, 1440, 1458, 1536, 1728, 1800, 1920, 1944, 2048]
```
## Prolog
```Prolog
main :- iter(1).
iter(K) :-
    (legal(K * x ^ 0) -> (maplist(write, [K, ', ']), flush_output) ; true),
    KK is K + 1, iter(KK).
legal(1 * x ^ 0).
legal(K * x ^ N) :-
    NN is N + 1, 0 is K mod NN, KK is K / NN,
    legal(KK * x ^ NN).
legal(K * x ^ N) :-
    ((K = 1, N = 1) ; (N > 1)), NN is N - 1,
    legal(K * x ^ NN).
% _Luc Rousseau_, Aug 20 2022
```
## Python
```Python
import heapq
from math import factorial
from sympy import nextprime
from itertools import islice
def agen(): # generator of terms
    oldv, h, primes, nextp, nextfact = 0, [(1, 1)], [], 0, 0
    while True:
        v, maxp = heapq.heappop(h)
        if v != oldv:
            yield v; oldv = v
            while nextfact < v:
                nextp = nextprime(nextp); nextfact = factorial(nextp)
                primes.append(nextp); heapq.heappush(h, (nextfact, nextp))
            for p in primes:
                if p <= maxp: heapq.heappush(h, (v*p, max(maxp, p)))
                else: break
print(list(islice(agen(), 60))) # _Michael S. Branicky_, Aug 20 2022
```
## PARI
```PARI
is(n) = if(n==1, return(1)); my(f = factor(n), p = f[#f~, 1]); n%p! == 0 \\ _David A. Corneth_, Sep 05 2022
```

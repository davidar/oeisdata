# a\(n\) is the least k\>1 such that omega\(k\) is equal to \(omega\(n\*k \+ 1\) \- 1\)/n\.
https://oeis.org/A350516
## JSON
```JSON
[5, 97, 443, 5801, 42697, 7813639, 10303967, 1225192093]
```
## Mathematica
```Mathematica
a[n_] := Module[{k = 2}, While[PrimeNu[k] != (PrimeNu[n*k + 1] - 1)/n, k++]; k]; Array[a, 5] (* _Amiram Eldar_, Mar 09 2022 *)
```
## PARI
```PARI
a(n) = my(k=2); while (omega(k) != (omega(n*k + 1) - 1)/n, k++); k; \\ _Michel Marcus_, Mar 09 2022
```
## Python
```Python
from sympy import factorint
for n in range(1, 8):
    for k in range(2, 10**10):
        if len(factorint(k).keys())*n+1==len(factorint(k*n+1).keys()):
            print(n, k)
            break # _Martin Ehrenstein_, Mar 14 2022
```

# Integers that are divisible by 4 and not by 5, and whose reverse is divisible by 5 and not by 4\.
https://oeis.org/A350232
## JSON
```JSON
[52, 56, 504, 508, 512, 516, 524, 528, 532, 536, 544, 548, 552, 556, 564, 568, 572, 576, 584, 588, 592, 596, 5004, 5008, 5012, 5016, 5024, 5028, 5032, 5036, 5044, 5048, 5052, 5056, 5064, 5068, 5072, 5076, 5084, 5088, 5092, 5096, 5104, 5108, 5112, 5116, 5124, 5128, 5132, 5136, 5144]
```
## Mathematica
```Mathematica
Select[4 * Range[1300], ! Divisible[#, 5] && Divisible[(r = IntegerReverse[#]), 5] && ! Divisible[r, 4] &] (* _Amiram Eldar_, Dec 21 2021*)
```
## PARI
```PARI
isok(k) = if (!(k%4) && (k%5) , k=fromdigits(Vecrev(digits(k))); !(k%5) && (k%4));
```
## Python
```Python
from itertools import count, islice
def A350232gen(): return filter(lambda n:(m := int(str(n)[::-1])) % 4 and not m % 5,filter(lambda n: n % 5 and not n % 4,count(1)))
A350232_list = list(islice(A350232gen(),20)) # _Chai Wah Wu_, Dec 21 2021
```

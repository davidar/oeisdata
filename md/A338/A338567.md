# Primes p such that \(q\*r\) mod p is prime, where q and r are the next primes after p\.
https://oeis.org/A338567
## JSON
```JSON
[3, 5, 7, 13, 19, 23, 31, 89, 199]
```
## Maple
```Maple
R:= NULL: q:= 2: r:= 3:
count:= 0:
for i from 1 to 10000 do
  p:= q; q:= r; r:= nextprime(r);
  if isprime(q*r mod p) then count:= count+1; R:= R, p fi
od:
R;
```
## Python
```Python
from sympy import nextprime, isprime
def afind(limit):
  p, q, r = 1, 2, 3
  while p < limit:
    p, q, r = q, r, nextprime(r)
    if isprime(q*r % p): print(p, end=", ")
afind(200) # _Michael S. Branicky_, Mar 05 2021
```

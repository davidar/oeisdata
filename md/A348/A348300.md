# a\(n\) is the largest number that is the digit sum of the square of an n\-digit number\.
https://oeis.org/A348300
## JSON
```JSON
[13, 31, 46, 63, 81, 97, 112, 130, 148, 162, 180, 193, 211, 229, 244, 262, 277, 297, 310, 331, 343, 360, 378, 396]
```
## Mathematica
```Mathematica
Array[Max@ Map[Total@ IntegerDigits[#^2] &, Range[10^(# - 1), 10^# - 1]] &, 8] (* _Michael De Vlieger_, Oct 12 2021 *)
```
## Sage
```Sage
def A348300(n):
    return max(sum((k^2).digits()) for k in (10^(n-1)..10^n-1))
```
## Python
```Python
def A348300(n): return max(sum(int(d) for d in str(m**2)) for m in range(10**(n-1),10**n)) # _Chai Wah Wu_, Jun 26 2024
```

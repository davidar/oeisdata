# a\(0\) \= 0; for n \>\= 1, a\(n\) \= A004185\(a\(n\-1\)\+n\)\.
https://oeis.org/A345681
## JSON
```JSON
[0, 1, 3, 6, 1, 6, 12, 19, 27, 36, 46, 57, 69, 28, 24, 39, 55, 27, 45, 46, 66, 78, 1, 24, 48, 37, 36, 36, 46, 57, 78, 19, 15, 48, 28, 36, 27, 46, 48, 78, 118, 159, 12, 55, 99, 144, 19, 66, 114, 136, 168, 129, 118, 117, 117, 127, 138, 159, 127, 168]
```
## Mathematica
```Mathematica
a[0] := 0;
```
```Mathematica
a[n_] := FromDigits[Sort[DeleteCases[IntegerDigits[a[n - 1] + n], 0]]];
```
```Mathematica
Table[a[n], {n, 0, 59}] (* _Robert P. P. McKone_, Aug 16 2021 *)
```
## Python
```Python
def A004185(n): return int("".join(sorted(str(n))).strip('0'))
def aupton(nn):
    alst = [0]
    for n in range(1, nn+1): alst.append(A004185(alst[-1]+n))
    return alst
print(aupton(100)) # _Michael S. Branicky_, Jun 22 2021
```
## PARI
```PARI
f(n) = fromdigits(vecsort(digits(n))); \\ A004185
a(n) = if (n==0, 0, f(a(n-1)+n)); \\ _Michel Marcus_, Jun 26 2021
```
```PARI
f(n) = fromdigits(vecsort(digits(n))); \\ A004185
lista(nn) = {my(v=vector(nn)); v[1] = 1; for (n=2, nn, v[n] = f(v[n-1]+n);); concat(0, v);} \\ _Michel Marcus_, Jun 26 2021
```

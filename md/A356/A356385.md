# First differences of A353654 which is numbers with the same number of trailing 0 bits as other 0 bits\.
https://oeis.org/A356385
## JSON
```JSON
[2, 4, 3, 5, 7, 4, 5, 5, 10, 8, 4, 5, 13, 8, 10, 6, 10, 8, 4, 5, 9, 20, 16, 8, 10, 14, 8, 10, 6, 10, 8, 4, 5, 25, 16, 20, 12, 20, 16, 8, 10, 10, 20, 16, 8, 10, 14, 8, 10, 6, 10, 8, 4, 5, 17, 40, 32, 16, 20, 28, 16, 20, 12, 20, 16, 8, 10, 26, 16, 20, 12, 20, 16]
```
## Mathematica
```Mathematica
Differences @ Join[{1}, Select[Range[2, 1000], IntegerExponent[#, 2] == Floor[Log2[# - 1]] - DigitCount[# - 1, 2, 1] &]] (* _Amiram Eldar_, Sep 21 2022 *)
```
## PARI
```PARI
Zeckendorf(n)=my(A=n, B, m=1, v=[1]); while(fibonacci(m+1)<=n, m++); m--; A-=fibonacci(m+1); while(m>1, m--; B=0; if(A-fibonacci(m+1)>=0, A-=fibonacci(m+1); B=1); v=concat(v,B)); v \\ A189920
b1(n)=local(m); if(n<1, 0, m=0; until(fibonacci(m)>n, m++); m-2) \\ A072649
b2(n)=my(A=concat(Zeckendorf(fibonacci(b1(n)+3)-n-1), 0), B, x=3, v=[1]); while(x<=#A, B=0; if(A[x]==1, x++; B=1); v=concat(v, 1-A[x-B]); x++); fromdigits(v, 2) \\ A247648 with rewrite 1 -> 1, 01 -> 0 applied to binary expansion
c1(n)=if(n<5, 4*(n-1) - hammingweight(n-1) + (n==1), my(A=2*b2(n-1) + 1); A*2^(logint(A, 2) - hammingweight(A) + 1)) \\ A353654
a(n)=c1(n+1) - c1(n)
```
```PARI
isok(k) = if (k==1, 1, (logint(k-1, 2)-hammingweight(k-1) == valuation(k, 2))); \\ A353654
lista(nn) = my(v=select(isok, [1..nn])); vector(#v-1, k, v[k+1] - v[k]); \\ _Michel Marcus_, Sep 21 2022
```
## Python
```Python
from itertools import pairwise, count, islice
def A356385_gen(): # generator of terms
    return map(lambda x:x[1]-x[0],pairwise(filter(lambda n:(~n & n-1).bit_length()<<1 == n.bit_length()-n.bit_count(),count(1))))
A356385_list = list(islice(A356385_gen(),30)) # _Chai Wah Wu_, Oct 14 2022
```

# Fixed points of A008473\.
https://oeis.org/A376299
## JSON
```JSON
[1, 4, 90, 120]
```
## Python
```Python
from sympy import factorint, prod
A008473 = lambda n: prod(sum(pk) for pk in factorint(n).items())
isok = lambda n: A008473(n) == n
print([n for n in range(1, 10**6) if isok(n)])
```

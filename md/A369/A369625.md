# Frobenius\-Perron dimensions of simple integral fusion rings of rank 4\.
https://oeis.org/A369625
## JSON
```JSON
[574, 7315, 63436, 65971, 68587, 90590, 113310, 310730, 311343, 494102, 532159, 585123]
```
## Python
```Python
# requires Normaliz version 3.10.2
import math
import PyNormaliz
from PyNormaliz import *
NmzSetNumberOfNormalizThreads(1)
def function(N):
    L = []
    sN1 = math.isqrt(N//3)
    sN = math.isqrt(N)
    for i1 in range(3, sN1):
        m1 = min(sN, N - i1**2, i1**2 + 1)
        for i2 in range(i1+1, m1):
            m2 = min(sN, N - i1**2 - i2**2, i2**2 + 1)
            for i3 in range(i2+1, m2):
                n = 1 + i1**2 + i2**2 + i3**2
                if n <= N:
                    C = Cone(fusion_type = [[1,i1,i2,i3]])
                    l = C.FusionRings()
                    if len(l)>0:
                        L.append(n)
    L.sort()
    return(L)
print(function(1000))
```

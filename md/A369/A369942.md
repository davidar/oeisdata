# a\(n\) is the number of distinct values of the determinant of an n X n Hankel matrix using the integers 1 to 2\*n \- 1\.
https://oeis.org/A369942
## JSON
```JSON
[1, 1, 3, 49, 1480, 50522, 2517213]
```
## Mathematica
```Mathematica
a[n_] := CountDistinct[Table[Det[HankelMatrix[Join[Drop[per = Part[Permutations[Range[2 n - 1]], i], n],{Part[per, n]}], Join[{Part[per, n]}, Drop[per, - n]]]], {i, (2 n - 1) !}]]; Join[{1}, Array[a, 5]]
```
## PARI
```PARI
a(n) = my(v=[1..2*n-1], list=List()); forperm(v, p, listput(list, matdet(matrix(n, n, i, j, p[i+j-1])));); #Set(list); \\ _Michel Marcus_, Feb 08 2024
```
## Python
```Python
from itertools import permutations
from sympy import Matrix
def A369942(n): return len({Matrix([p[i:i+n] for i in range(n)]).det() for p in permutations(range(1,n<<1))}) # _Chai Wah Wu_, Feb 12 2024
```

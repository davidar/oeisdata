# Numbers without 4 as a digit\.
https://oeis.org/A052406
## JSON
```JSON
[0, 1, 2, 3, 5, 6, 7, 8, 9, 10, 11, 12, 13, 15, 16, 17, 18, 19, 20, 21, 22, 23, 25, 26, 27, 28, 29, 30, 31, 32, 33, 35, 36, 37, 38, 39, 50, 51, 52, 53, 55, 56, 57, 58, 59, 60, 61, 62, 63, 65, 66, 67, 68, 69, 70, 71, 72, 73, 75, 76, 77, 78, 79, 80, 81, 82, 83, 85, 86, 87, 88, 89]
```
## Maple
```Maple
a:= proc(n) local l, m; l, m:= 0, n-1;
      while m>0 do l:= (d->
        `if`(d<4, d, d+1))(irem(m, 9, 'm')), l
      od; parse(cat(l))/10
    end:
seq(a(n), n=1..100);  # _Alois P. Heinz_, Aug 01 2016
```
## Mathematica
```Mathematica
Select[Range[100], DigitCount[#, 10, 4] == 0 &] (* _Alonso del Arte_, Oct 13 2013 *)
```
## PARI
```PARI
g(n)= local(x,v,j,flag); for(x=1,n, v=Vec(Str(x)); flag=1; for(j=1,length(v), if(v[j]=="4",flag=0)); if(flag,print1(x",") ) ) \\ _Cino Hilliard_, Apr 01 2007
```
```PARI
apply( {A052406(n)=fromdigits(apply(d->d+(d>3),digits(n-1,9)))}, [1..99]) \\ a(n)
select( {is_A052406(n)=!setsearch(Set(digits(n)),4)}, [0..99]) \\ Used in A038612
next_A052406(n, d=digits(n+=1))={for(i=1, #d, d[i]!=4|| return((1+n\d=10^(#d-i))*d)); n} \\ least a(k) > n. Used for A038612. - _M. F. Hasler_, Jan 11 2020
```
## Magma
```Magma
[ n: n in [0..89] | not 4 in Intseq(n) ]; // _Bruno Berselli_, May 28 2011
```
## Shell
```Shell
seq 0 1000 | grep -v 4; # _Joerg Arndt_, May 29 2011
```
## Haskell
```Haskell
a052406 = f . subtract 1 where
f 0 = 0
f v = 10 * f w + if r > 3 then r + 1 else r where (w, r) = divMod v 9
-- _Reinhard Zumkeller_, Oct 07 2014
```
## Python
```Python
def A052406(n): n-=1; return sum((d+(d>3))*10**i for d,i in ((n//9**i%9,i) for i in range(math.ceil(math.log(n+1,9))))) # _M. F. Hasler_, Jan 13 2020
```

# Most significant bit of n, msb\(n\); largest power of 2 less than or equal to n; write n in binary and change all but the first digit to zero\.
https://oeis.org/A053644
## JSON
```JSON
[0, 1, 2, 2, 4, 4, 4, 4, 8, 8, 8, 8, 8, 8, 8, 8, 16, 16, 16, 16, 16, 16, 16, 16, 16, 16, 16, 16, 16, 16, 16, 16, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 32, 64, 64, 64, 64, 64, 64, 64, 64, 64, 64, 64]
```
## Maple
```Maple
a:= n-> 2^ilog2(n):
seq(a(n), n=0..80);  # _Alois P. Heinz_, Dec 20 2016
```
## Mathematica
```Mathematica
A053644[n_] := 2^(Length[ IntegerDigits[n, 2]] - 1); A053644[0] = 0; Table[A053644[n], {n, 0, 74}] (* _Jean-François Alcover_, Dec 01 2011 *)
```
```Mathematica
nv[n_] := Module[{c = 2^n}, Table[c, {c}]]; Join[{0}, Flatten[Array[nv, 7, 0]]] (* _Harvey P. Dale_, Jul 17 2012 *)
```
## Haskell
```Haskell
a053644 n = if n <= 1 then n else 2 * a053644 (div n 2)
-- _Reinhard Zumkeller_, Aug 28 2014
a053644_list = 0 : concat (iterate (\zs -> map (* 2) (zs ++ zs)) [1])
-- _Reinhard Zumkeller_, Dec 08 2012, Oct 21 2011, Oct 17 2010
```
## PARI
```PARI
a(n)=my(k=1);while(k<=n,k<<=1);k>>1 \\ _Charles R Greathouse IV_, May 27 2011
```
```PARI
a(n) = if(!n, 0, 2^exponent(n)) \\ _Iain Fox_, Dec 10 2018
```
## Python
```Python
def a(n): return 0 if n==0 else 2**(len(bin(n)[2:]) - 1) # _Indranil Ghosh_, May 25 2017
```
```Python
def A053644(n): return 1<<n.bit_length()-1 if n else 0 # _Chai Wah Wu_, Jul 27 2022
```
## Magma
```Magma
[0] cat [2^Ilog2(n): n in [1..90]]; // _Vincenzo Librandi_, Dec 11 2018
```
## Scala
```Scala
(0 to 127).map(Integer.highestOneBit(_)) // _Alonso del Arte_, Feb 26 2020
```
